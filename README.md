# Git GUI

Git GUI client. Currently supports only some basic Git functions. The backend is just a shell provider, so the web frontend can access Git. It does not have any security checks.

Outputs of some Git commands are parsed algorithmically.

This project is in a very early stage, and at the moment I'm doing huge refactoring, and moving code into components, so the application shifts from the experimental phase to a more production-ready one.